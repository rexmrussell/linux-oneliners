Listen on a specific port:
  nc -l -p 5000
  
Connect to NC on a specific port:
  nc -v domain.com 5000
  nc -v IPaddress 5000
  
Check which ports are listening on localhost:
  nmap -sT -O localhost
  
On Ubuntu 16, open port:
  ufw allow 5000

Show routing table:
  route -n
  netstat -rn
  ip route list
